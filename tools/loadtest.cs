using System; using System.Reflection; class T { static int Main() { try { Assembly a = Assembly.LoadFrom(@"D:\豆包的下载\Machine_Dev\tools\cecil\lib\net40\Mono.Cecil.dll"); Console.WriteLine("LOADED " + a.FullName); Type t = a.GetType("Mono.Cecil.AssemblyDefinition"); Console.WriteLine("TYPE " + t); } catch (Exception e) { Console.WriteLine("ERR type=" + e.GetType().FullName); Console.WriteLine("MSG=" + e.Message); Exception i = e.InnerException; while (i != null) { Console.WriteLine("INNER=" + i.GetType().FullName + " MSG=" + i.Message); i = i.InnerException; } return 2; } return 0; } }
